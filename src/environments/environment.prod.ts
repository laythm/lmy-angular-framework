export const environment = {
  production: true,
  api_url:"http://localhost:44357/api"//
  //go to https://github.com/laythm/lmy-netcore-api-framework and download then run it has required api's to run this application
};
